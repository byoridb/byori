Use case: ui-mockup
Asset type: high-fidelity desktop app composition for Byori, a native macOS multi-agent coding workspace
Primary request: Create COMPOSITION B, a terminal-first first viewport for a real native macOS SwiftUI app. The hierarchy is Project → Source Tree or Worktree → Task → Session. Each session has exactly one user-chosen coding agent and model, fixed for its lifetime. Multiple agents are available across sessions, but there is no automatic same-task fan-out.
Scene/backdrop: one macOS application window shown straight-on and edge to edge; no physical device, browser, or surrounding environment
Subject: a compact three-column workspace. Left: a narrow 215px outline headed "Projects" with project "byori" expanded, source tree "main", task "Build workspace", and two manually-created session rows, with "Codex · gpt-5.6" selected and "Claude · Sonnet" inactive beneath it. A clear small "New Session" action and Settings gear at the bottom. Center: at least 65 percent of the window, a large dark interactive terminal. Above the terminal is a thin native tab strip for task sessions; only the selected terminal is visible. The selected tab includes a running status dot and fixed agent/model label. A small toolbar contains Stop and a plus button, without agent broadcast controls. Right: a narrow collapsible inspector with tabs "Files", "Git", and "Context"; Git is active, showing branch "main", a clean status, and a short changed-files list area.
Style/medium: polished high-fidelity native macOS SwiftUI UI; standard title bar and traffic lights; system typography and native controls; compact density, crisp 1px separators, subtle material; no custom illustrations
Composition/framing: 16:10 landscape, one full app viewport, narrow navigator / very large terminal / narrow inspector; emphasize useful vertical terminal space
Lighting/mood: neutral light macOS chrome with a dark near-black terminal; focused and professional
Color palette: macOS gray and white, near-black terminal, restrained teal for selected/running state, semantic green only for clean state
Text (verbatim): "Byori", "Projects", "byori", "main", "Build workspace", "Codex · gpt-5.6", "Claude · Sonnet", "New Session", "Stop", "Files", "Git", "Context", "Clean", "Settings"
Constraints: show exactly one terminal viewport; the second agent is merely another manually-created session row/tab, never a concurrent comparison pane; project hierarchy must be clear; agent/model metadata belongs to each session; all core labels readable; use compact native spacing; Settings stays outside primary content
Avoid: multi-terminal split view, automatic fan-out, broadcast prompt, agent leaderboard, combined conversation timeline, dashboard cards, large rounded cards, web-app sidebar styling, gradients, purple neon, copied Orca or Paseo branding, marketing copy, fake metrics, watermarks
